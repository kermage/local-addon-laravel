import React from 'react';

import { Text } from '@getflywheel/local-components';

export default function () {
	return {
		label: 'Create a Laravel site',
		content: <Text>Spin up a new Laravel Site</Text>,
	};
}
